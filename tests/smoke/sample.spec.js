import { test, expect } from '@playwright/test';

test.describe("Sample Test",{tags:["sample"]},()=>{
    
    test('TC001', async ({ page }) => {
        
    });

    test('TC002', async ({ page }) => {
        
    });

    test('TC003', async ({ page }) => {
        
    });

});